name: 🐛 バグ報告
description: バグや不具合を報告する
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        バグ報告ありがとうございます。以下のフォームに記入してください。

  - type: textarea
    id: description
    attributes:
      label: バグの説明
      description: 何が起きたか、期待される動作は何かを説明してください
      placeholder: タスクを作成しようとすると、アプリがクラッシュする
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 再現手順
      description: バグを再現するための手順を記載してください
      placeholder: |
        1. ホーム画面で「+」ボタンをタップ
        2. タイトルを入力
        3. 時刻を選択
        4. 「保存」をタップするとクラッシュ
    validations:
      required: true

  - type: dropdown
    id: platform
    attributes:
      label: プラットフォーム
      description: どのプラットフォームで発生しましたか？
      options:
        - Android
        - iOS
        - Desktop (Windows)
        - Desktop (macOS)
        - Desktop (Linux)
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: アプリバージョン
      description: アプリのバージョン（設定画面で確認可能）
      placeholder: "1.0.0"

  - type: textarea
    id: additional
    attributes:
      label: 追加情報
      description: その他の情報やスクリーンショットがあれば記載してください
